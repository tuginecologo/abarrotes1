// middlewares/core.js
// const express = require('express');

// module.exports = (app) => {
  // Body parsers
  // app.use(express.urlencoded({ extended: true }));
  // app.use(express.json());
  
  // Add other universal middlewares here
  // app.use(yourMiddleware);
// };